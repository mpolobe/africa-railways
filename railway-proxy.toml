# Railway configuration for Caddy reverse proxy service
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.proxy"

[deploy]
startCommand = "caddy run --config /etc/caddy/Caddyfile --adapter caddyfile"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
PORT = "8080"
