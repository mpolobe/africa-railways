<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mpolobe.africoin">

    <!-- ============================================ -->
    <!-- PERMISSIONS -->
    <!-- ============================================ -->

    <!-- Essential Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Camera for QR Code Scanning -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    
    <!-- Biometric Authentication -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    
    <!-- Location for Train Tracking (Optional) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    
    <!-- Notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <!-- Background Services for Real-Time Tracking -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Storage for Offline Tickets -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
        android:maxSdkVersion="28" />
    
    <!-- Network State for Offline Mode -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- ============================================ -->
    <!-- APPLICATION -->
    <!-- ============================================ -->

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true">

        <!-- ============================================ -->
        <!-- MAIN ACTIVITY -->
        <!-- ============================================ -->

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true"
            android:screenOrientation="portrait">
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep Links for Train Tracking -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="africarailways.com"
                    android:pathPrefix="/track" />
                <data
                    android:scheme="africoin"
                    android:host="track" />
            </intent-filter>

            <!-- Deep Links for Tickets -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="africarailways.com"
                    android:pathPrefix="/ticket" />
                <data
                    android:scheme="africoin"
                    android:host="ticket" />
            </intent-filter>

            <!-- Deep Links for Payments -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="africarailways.com"
                    android:pathPrefix="/pay" />
                <data
                    android:scheme="africoin"
                    android:host="pay" />
            </intent-filter>
        </activity>

        <!-- ============================================ -->
        <!-- SERVICES -->
        <!-- ============================================ -->

        <!-- Train Tracking Service -->
        <service
            android:name=".services.TrainTrackingService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location">
            <intent-filter>
                <action android:name="com.mpolobe.africoin.TRAIN_TRACKING" />
            </intent-filter>
        </service>

        <!-- Notification Service -->
        <service
            android:name=".services.NotificationService"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- ============================================ -->
        <!-- RECEIVERS -->
        <!-- ============================================ -->

        <!-- Boot Receiver for Background Tracking -->
        <receiver
            android:name=".receivers.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Network Change Receiver -->
        <receiver
            android:name=".receivers.NetworkChangeReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>

        <!-- ============================================ -->
        <!-- PROVIDERS -->
        <!-- ============================================ -->

        <!-- File Provider for Sharing Tickets -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- ============================================ -->
        <!-- METADATA -->
        <!-- ============================================ -->

        <!-- Google Play Services -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!-- Firebase Cloud Messaging -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_color" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="train_tracking_channel" />

        <!-- OCC Telemetry Configuration -->
        <meta-data
            android:name="com.africarailways.occ.telemetry.enabled"
            android:value="true" />
        <meta-data
            android:name="com.africarailways.occ.telemetry.project_id"
            android:value="africa-railways-481823" />
        <meta-data
            android:name="com.africarailways.occ.telemetry.service_account"
            android:value="africoin-wallet-deploy@gen-lang-client-0453426956.iam.gserviceaccount.com" />
        <meta-data
            android:name="com.africarailways.occ.telemetry.api_endpoint"
            android:value="https://api.africarailways.com/v1/telemetry" />

        <!-- TAZARA Corridor Configuration -->
        <meta-data
            android:name="com.africarailways.corridor.tazara.enabled"
            android:value="true" />
        <meta-data
            android:name="com.africarailways.corridor.tazara.route_id"
            android:value="DAR-KMP-1860" />
        <meta-data
            android:name="com.africarailways.corridor.tazara.stations"
            android:value="DAR,MOR,KIL,MBY,TUN,NAK,KMP" />

        <!-- App Version -->
        <meta-data
            android:name="com.africarailways.app.version"
            android:value="1.0.0" />
        <meta-data
            android:name="com.africarailways.app.build"
            android:value="1" />

    </application>

    <!-- ============================================ -->
    <!-- QUERIES (Android 11+) -->
    <!-- ============================================ -->

    <queries>
        <!-- Mobile Money Apps -->
        <package android:name="com.vodacom.mpesa" />
        <package android:name="com.airtel.money" />
        <package android:name="com.mtn.momo" />
        
        <!-- Banking Apps -->
        <package android:name="com.crdb.banking" />
        <package android:name="com.nmbbank" />
        <package android:name="com.zanaco" />
        
        <!-- Maps for Station Navigation -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>
        
        <!-- Browser for External Links -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        
        <!-- Email for Support -->
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
        
        <!-- Phone for Support -->
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <data android:scheme="tel" />
        </intent>
    </queries>

</manifest>
